import { boot } from 'quasar/wrappers'
// API client is self-configuring via auth-store; this boot file is a placeholder
// for any future global axios configuration (e.g., base URL from runtime config).
export default boot(() => {})
